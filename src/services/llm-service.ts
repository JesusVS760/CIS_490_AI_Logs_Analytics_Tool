// type LlmResponse = {

// }

export const llmService = {
  async generateAnalytics(text: string) {
    try {
      const systemPrompt = `
            INSTRUCTIONS: 
        
        `;
    } catch (error) {}
  },
};
